/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Configure domains if you need to use external image sources
    domains: ['images.unsplash.com', 'd322abd807wvow.cloudfront.net', 'd3q0oijmiduzg2.cloudfront.net'],
  },
}

module.exports = nextConfig