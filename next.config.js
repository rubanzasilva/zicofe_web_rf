/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Configure domains if you need to use external image sources
    // domains: ['example.com'],
  },
}

module.exports = nextConfig